import { Space_Mono, Inter } from "next/font/google";

export const MONO_FONT = Space_Mono({
  subsets: ["latin"],
  weight: "400",
});

export const SANS_FONT = Inter({
  subsets: ["latin"],
  weight: "400",
});
